django-admin
django-admin help <subcommand>

py manage.py

and Type 'manage.py help <subcommand>' for help on a specific subcommand.

# three ways to get data from the form it is possible that there is more ways other than this

request.method == "get":

    request.GET.get("name")

request.method == "post":

    request.POST.get("name")
    
    request.POST["name"]

if making website in the main project without starting new app 

add templates as shown below 
> setting.py > templates section > dir = ["templates"] 

